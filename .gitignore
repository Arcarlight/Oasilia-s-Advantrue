# ---- GitHub Pages 不需要的东西 ----
# 第三方原始素材包（Kenney 的素材包，只用来「生成」assets/ 里的东西；
# 游戏运行时完全不读它们。来源与挑选规则见 README「素材来源」与 tools/copy-kenney.mjs）
kenney_*/

# 工作区里那份没被任何代码引用的字体（游戏用的是 assets/fonts/LXGWNeoXiHeiPlus.ttf）
森泽UD新黑 Gb4 R.otf

# 三套新字体的**原始文件**（合计 46MB）：只用来「生成」assets/fonts/ 里按用到的字
# 裁出来的子集（tools/subset-fonts.mjs），运行时读的是子集。
# 不 ignore 的话 git add -A 会把 46MB 卷进仓库，而线上站点根本用不到它们。
SGHr-Regular.ttf
WenYuanSerifSC-Bold.ttf
851LakeusNightWriting-Regular.ttf
# 手写体的现任（写意体 SC）与上一任（851 远星夜行）都是**构建输入**：
# 只有 tools/subset-fonts.mjs 会把它们裁成 assets/fonts/ 里的子集，运行时读的是子集。
YShiWrittenSC-Regular.ttf

# 日语的那三套（用户提供：粗体 / 正文 / 手写），同样是构建输入：
# 只用来裁出 assets/fonts/ 里的日语子集（约 512KB），运行时读的是子集。
はなぞめフォント.otf
XiaoshanCircle.ttf
YOzBS_.otf

# Generation 9 Pack 的原始压缩包（58 MB）：只用来「生成」assets/gen9/ 里的立绘，
# 而且是从 %TEMP% 解包后由 tools/import-gen9.mjs 挑图，运行时完全不读它。
# 顺手加一条 *.rar 兜住别的素材包，免得再被 git add -A 卷进仓库。
*.rar
*.zip
*.7z

# 诊断产物 / 截图 / 浏览器缓存（体积大，与运行无关）
tools/shots/
tools/.edge-profile*/
verify-bundle.html

# 注意：tools/animdata-cache/ **不进这里**（以前被当成「缓存」忽略掉了）。
# 它是 SpriteCollab 每个物种的 AnimData.xml（帧宽 / 帧高），
# `assets/data/sprites.json` 里的帧尺寸就是从这里读的 —— 少了它，生成脚本只能「猜」，
# 而猜出来的帧尺寸会让一格里塞进两三帧（画面上就是两只宝可梦并排跳，
# 用户反馈的「赤面龙、电龙的行走图有问题」就是这么来的）。
# 111 份合计 795 KB，和已提交的 sprites.json 是一体的：要提交一起提交。

# BGM 一律用 .ogg（上游的 ogg(L) 版带无缝循环注释，mp3 版接缝处有编码器补的静音）。
# 旧版留下的 mp3 已经删干净了，这条留着防止哪天又下回来 —— 那种文件没人引用，
# 却会让站点多出几十 MB。check-content 也会把 .mp3 直接当错误报。
assets/audio/bgm/*.mp3

# 多智能体协作的中间状态
.agent-teams/

# 杂项
*.log

# 提交信息临时文件（别跟着提交进去）
.git-commit-msg.txt
.commit-msg-2.txt
