曲子库整页没跟着语言走（用户截图指出）

用户截了日语界面下的曲子库：分组标题、「第一章 流沙之海」、授权说明全是中文，
而覆盖率报告还写着 100%。

根因（这一类 bug 的通用形态）：
- tools/build-i18n.mjs 只认 `t('字面量')`。写在对象里的裸字符串（`{ map: '地图 · 一章一曲' }`）
  永远进不了待翻清单 —— 所以覆盖率 100% 与「界面全是中文」可以同时成立。
- 场景名是 content/bgm.json 的 desc **拼出来**的（`第一章 流沙之海`、`流沙之海的战斗`、
  `标题 · 旅途开始`），词典里没有整串条目，`t(整串)` 一定退中文，
  连里面本来有译文的「流沙之海」也一起被拖住。
- 授权说明来自生成的 BGM_CREDITS（不是 i18n 表），切语言时没人改写它。

修法：
- music-room.js：分组标题与场景名全部改成**带字面量的 t()**，并且用 getter 在渲染时求值
  （写成模块级常量会把首次加载的语言焊死 —— 切语言后标题还是中文）。
  场景名走 SCENE_LABEL 表（38 条字面量：章节名 / 地图名 / 战斗 / 强敌 / 各种兜底曲），
  外加「章节号 + 地图名」的兜底拆分。
- build-content.mjs：BGM_LICENSES（授权原文）单独摘出来，挂进 i18n-tables 的
  I18N_WORD_TABLES，切语言时会被 applyContentLang 原地改写。
- codex.js 的奖牌名 / ui.js 的一条 toast 也补上 t()。
- 新增 tools/audit-ui-strings.mjs：扫「含中文但没走 t()」的字符串字面量（人工排查这一类漏网用）。
- diag-music 新增第 ⑥ 节：切到日语后打开曲子库，分组标题 / 章节号 / 地图名 / 锁定文案
  逐条核对（只查简体专有字，避免把「戦闘・画面」这类正常日文汉字误报）。

验证：check-content --strict ✓ · check-copy 7/7 ✓ · 11 套回归 ✓ · diag-music 全过 ✓ ·
diag-codex 105 条 ✓ · VERIFY_OK · SMOKE_OK。更新日志 v1.14 + package.json 1.14；
字体子集重裁；ja/en 2492/2492（100%）。
